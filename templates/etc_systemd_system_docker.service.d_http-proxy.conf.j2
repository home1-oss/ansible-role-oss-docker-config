[Service]
Environment="NO_PROXY=localhost,127.0.0.0/8" {% if http_proxy is defined and http_proxy %}"HTTP_PROXY={{ http_proxy }}"{% endif %} {% if https_proxy is defined and http_proxy %}"HTTPS_PROXY={{ https_proxy }}"{% endif %}
