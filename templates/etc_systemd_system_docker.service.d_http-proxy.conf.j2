[Service]
Environment="NO_PROXY=localhost,127.0.0.0/8{% if docker_insecure_registries is defined and docker_insecure_registries %}{% for registry in docker_insecure_registries %},{{ registry.split(':')[0] }}{% endfor %}{% endif %}" {% if http_proxy is defined and http_proxy %}"HTTP_PROXY={{ http_proxy }}"{% endif %} {% if https_proxy is defined and http_proxy %}"HTTPS_PROXY={{ https_proxy }}"{% endif %}
